This folder contains the scripts to compile the fpga design.

In linux:

vivado -mode batch -source setup.tcl
vivado -mode batch -source compile.tcl

In Windows:

Open xtcl shell.

source setup.tcl
source compile.tcl

